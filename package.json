{
	"description": "",
	"repository": {},
	"license": "Unlicense",
	"devDependencies": {
		"typescript": "4.9.4"
	},
	"dependencies": {
		"preact": "10.11.3"
	},
	"scripts": {
		"build": "tsc",
		"serve": "npx http-server ./app",
		"vendor": "npm ci --ignore-scripts && cd build && npm ci --ignore-scripts && npm run vendor"
	}
}
