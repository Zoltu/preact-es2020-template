{
	"description": "",
	"repository": {},
	"license": "Unlicense",
	"devDependencies": {
		"typescript": "5.0.4"
	},
	"dependencies": {
		"@preact/signals": "1.1.3",
		"preact": "10.13.2"
	},
	"scripts": {
		"setup": "npm ci --ignore-scripts && tsc && cd build && npm ci --ignore-scripts && npm run vendor",
		"build": "tsc",
		"watch": "tsc --watch",
		"serve": "npx http-server ./app",
		"vendor": "cd build && npm run vendor"
	}
}
